// This component has been disabled to prevent WebGL crashes
// on devices that don't support WebGL or have it disabled

import React from 'react';

const StyledStarsCanvas = () => {
  return null;
};

export default StyledStarsCanvas;
